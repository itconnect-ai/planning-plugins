# 유지보수 가이드 (운영자용)

이 문서는 플러그인을 수정·배포하는 운영자를 위한 규칙입니다. 학습자용 안내는 [README.md](README.md)를 보세요.

## 두 플러그인 동기화 규칙 (가장 중요)

같은 교육 과정이 두 벌로 존재합니다. **스킬 내용을 수정하면 반드시 양쪽에 반영합니다.**

| | Claude Code (`plugins/vibe-pipeline`) | Codex (`plugins/vibe`) |
|---|---|---|
| 파일 상태 확인 | `## 지금 상태`의 `` !`cat ...` `` 셸 주입 — 프로그램이 실행 | `## 시작 절차`의 절차 지시 — 모델이 도구로 직접 확인 |
| 사용자 입력 | `$ARGUMENTS` 치환 | "스킬 호출 뒤에 함께 입력된 문장" 지시 |
| 자동 발동 금지 | frontmatter `disable-model-invocation: true` | 스킬별 `agents/openai.yaml`의 `allow_implicit_invocation: false` |
| 명령 형식 | `/vibe:01-prd` | `$vibe:01-prd` |

내용 수정 시 위 형식 차이만 번역하고 규칙·문구는 동일하게 유지합니다.

## 바꾸면 안 되는 것 (다른 스킬이 파싱하는 형식)

- **산출물 경로·파일명**: `docs/setup.md`, `docs/prd.md`, `docs/design-prompt.md`, `docs/frd.md`, `docs/trd.md`, `docs/plan.md`, 시안은 `design/01-home.png` 형식. 모든 스킬이 이 경로를 하드코딩으로 읽습니다.
- **plan.md 체크박스**: `- [ ]` / `- [x]` — build가 읽고 갱신하며, status가 `^- \[` 패턴으로 집계합니다.
- **STORY 완료 기록**: 체크박스 줄 바로 아래 들여쓴 `  - 기록: ...` 줄. 체크박스 줄 자체의 형식은 바꾸지 않습니다.
- **기능 ID**: FRD 기능 트리의 `F1`, `F1.2` — 05-plan의 STORY 태그 `[F1.2]`와 커버리지 점검이 참조합니다.
- **문서 상태 줄**: prd, design-prompt, frd, trd, plan **다섯 문서 전체**의 제목 아래 `상태: 작성 중 (다음: ...)` / `상태: 완료` — 각 스킬의 이어가기와 status의 완료 판단이 참조합니다. 상태 줄이 없는 기존 문서는 호환성을 위해 완료로 간주합니다.
- **setup.md 형식**: `- 진행 모드: 꼼꼼|간단`, `- 아이디어 한 줄: ...` — 01~05가 읽습니다.

## 핵심 정책 (모든 스킬 공통)

- 진행 모드는 시간이 아니라 **대화 방식**: 꼼꼼(학습 중심) / 간단(핵심 결정만). 어느 모드든 만들어지는 범위와 결과물은 같다.
- 추천은 **결과물 우선** — 쉽거나 무난하다는 이유로 낮춰 추천하지 않는다.
- 모든 선택지에 "고르면:"(결과 미리보기 한 줄)을 붙인다.
- "네/아무거나/모르겠어요" → 추천 채택 + `(추천 수용)` 표시. 2회 연속 시 위임 제안. 위임 후에도 제품의 모습·범위를 정하는 필수 결정은 멈춰서 묻는다.
- 화면에서 차이가 보이지 않는 결정은 묻지 않는다. 단, **기능 유무·범위 축소는 반드시 묻는다** — 임의로 부가/다음 버전으로 미루지 않는다.
- **학습자 승인 전에는 문서·범위를 수정하지 않는다** — 기능 제외, 부가로 밀기, 문서 충돌 수정 모두 승인 후에만 반영한다.
- 범위 판단 기준은 기간이 아니라 **"1인 AI 개발 MVP" 복잡도**다(사용자 역할 수, 외부 연동, 결제·실시간, 권한·데이터 관계). 기능 수·화면 수는 가이드라인 — 초과 시 영향 설명 후 학습자 선택 존중, `(기준 초과 선택)` 표시. "3개월" 같은 기간 기준은 쓰지 않는다.
- `(추천 수용)`은 사용자가 위임해 확정한 결정이고, `(추정)`만 미확정 항목이다.
- 시안 이미지(`design/`)는 선택 산출물이다 — 2단계 완료 기준은 design-prompt.md의 상태 줄이며, 이미지가 없어도 3단계를 막지 않는다.
- 사람 기준 개발 시간(반나절·하루)을 산정하거나 약속하지 않는다. 진행 단위는 STORY 개수.
- 완료 기준은 로컬 실행(localhost). 배포는 마지막 "배포(선택)" EPIC.
- 업무 주제는 실데이터 금지·가짜 데이터, 공개 데이터 주제는 출처 사용 가능.

## 검증과 배포

1. 수정 후 저장소 루트에서: `claude plugin validate . --strict`
2. Codex 쪽은 CLI 검증 도구가 없으므로 Codex 환경에서 `$vibe:01-prd`를 실제 실행해 확인합니다.
3. 릴리스: 두 plugin.json(`plugins/vibe-pipeline/.claude-plugin/plugin.json`, `plugins/vibe/.codex-plugin/plugin.json`)의 버전을 **같은 값으로 동시에** 올리고 커밋·푸시합니다. **버전을 올리지 않으면 학습자 쪽에서 업데이트가 감지되지 않습니다** — 스킬 내용을 고쳤으면 반드시 올립니다.
4. 학습자 업데이트: Claude Code는 `claude plugin marketplace update itconnect` → `claude plugin update vibe@itconnect` → 재시작. (`install`은 업데이트가 아닙니다 — 이미 설치된 경우 아무것도 바뀌지 않습니다.) Codex는 `codex plugin marketplace upgrade itconnect` → `codex plugin add vibe@itconnect` → 새 작업 열기.
5. 강의 투입 전 파일럿: 빈 폴더에서 설치 → `01-prd`부터 `build` 2~3개까지 실제로 완주해 봅니다.

## 수정 시 체크리스트

- [ ] 양쪽 플러그인에 같은 내용 반영했는가
- [ ] 위 "바꾸면 안 되는 것"의 형식을 건드리지 않았는가
- [ ] `claude plugin validate . --strict` 통과했는가
- [ ] README 3개(루트, 각 플러그인)의 설명과 어긋나지 않는가
- [ ] 버전을 올렸다면 두 plugin.json 모두 같은 값인가
